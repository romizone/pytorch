import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PyTorch Neural Network Simulation | Interactive ML Learning",
  description: "Interactive simulation explaining PyTorch neural networks with live training, backpropagation visualization, and XOR problem solving.",
  keywords: ["PyTorch", "Neural Network", "Machine Learning", "Deep Learning", "Simulation", "Interactive"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
